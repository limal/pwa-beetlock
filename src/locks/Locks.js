import React from "react";
import "../css/Locks.scss";

export const Locks = () => {
  return (
    <div className="Locks">
      Locks protected you need to be logged in to see this
    </div>
  );
};
