import React from "react";
import { Link } from "react-router-dom";

export const Logo = () => (
  <div className="Logo">
    <Link to="/" className="Logo-Link">
      WB LOCK
    </Link>
  </div>
);
